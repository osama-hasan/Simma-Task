# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Simma Task' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Simma Task

  pod 'SwiftSoup'
  pod 'Kingfisher', '~> 7.0'
  target 'Simma TaskTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Simma TaskUITests' do
    # Pods for testing
  end

end
